import MainBanner from "./MainBanner";
import MatchList from "./MatchList";

export default function MainPage() {
  return (
    <section>
      <MainBanner />
      <MatchList />
    </section>
  );
}
